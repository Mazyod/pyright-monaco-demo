{
  "name": "pyright-monaco-demo",
  "version": "2.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm i && npm run build:web && cd ../server && npm i && npm run build && cd .."
  }
}
